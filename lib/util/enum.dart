enum APIRequestStatus {
  unInitialized,
  loading,
  loaded,
  error,
  connectionError,
}

enum EnumDisplay {
  grid,
  list
}

enum EnumSort {
  latest,
  outstanding,
}
enum ActionBook {
  details,
  delete,
}
